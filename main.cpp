#include <algorithm>
#include <unordered_map>
#include <vector>

#include <gtest/gtest.h>

#include <rapidjson/document.h>

using namespace std;

TEST(MySuite, case1)
{
    //First test case
    ASSERT_TRUE(true);
}

TEST(MySuite, case2)
{
    //First test case
    ASSERT_TRUE(true);
}